  steps:
    # 📝 Step 1: Fetch your template skeleton cleanly from GitHub
    - id: fetch-base
      name: Fetch Base Skeleton
      action: fetch:template
      input:
        url: https://github.com/IvayloB84/backstage-pilot/tree/main/catalog/skeleton
        values:
          repoName: ${{ parameters.repoName }}
          repoOwner: ${{ parameters.repoOwner }}
          description: ${{ parameters.description }}

    # 🔗 Step 2: Publish the generated catalog file directly to a new GitHub repo
    - id: publish
      name: Publish to GitHub
      action: publish:github
      input:
        repoUrl: 'github.com?repo=${{ parameters.repoName }}&owner=${{ parameters.repoOwner }}'
        repoVisibility: public
        description: ${{ parameters.description }}
        defaultBranch: main

    # 📁 Step 3: Register the new repository location back into your Backstage catalog
    - id: register
      name: Register in Backstage Catalog
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps.publish.output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml


  output:
    links:
      - title: Open GitHub Repository
        url: ${{ steps.publish.output.remoteUrl }}