apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: argo-live-service-final
  description: Live GitOps application microservice tracking over k3s loopbacks.
  annotations:
    argocd/app-name: argo-live-service-final
    # Links Backstage's resource tree-grouper directly to the deployment name
    backstage.io/kubernetes-id: argo-live-service-final-deployment
    backstage.io/kubernetes-namespace: default
    backstage.io/kubernetes-label-selector: 'app=argo-live-service-final'
spec:
  type: service
  lifecycle: experimental
  owner: user:default/IvayloB84

---
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: IvayloB84
spec:
  profile: {}
  memberOf: [guests]
